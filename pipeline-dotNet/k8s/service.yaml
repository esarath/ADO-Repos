apiVersion: v1
kind: Service
metadata:
  name: pipeline-dotnet
  namespace: ado-pipeline
  labels:
    app: pipeline-dotnet
spec:
  selector:
    app: pipeline-dotnet
  ports:
  - name: http
    port: 80
    targetPort: 8080
